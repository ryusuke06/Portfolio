<header>
  <%= render "tops/header" %>
</header><!-- /header -->

<!-- 追従するサイドバー　シェアボタンとか載せたいなあ -->
<div class="sidebar">
  <div class="sticked">
  	<p>aaa</p>
  </div>
</div>

  <div class="container">
    <main role="main">
      <!-- ジャンボトロン -->
      <div class="jumbotron text-center">
        <%= attachment_image_tag @test, :image_id, size: "280x190", format: "jpeg", fallback: "no_image.jpg" %>
        <h1 class="display-4"><%= @test.title %></h1>
        <p class="lead"><%= @test.caption %></p>
        <%= link_to "診断を開始する", test_details_path(test_id: @test.id), class: "btn btn-success btn-lg" %>
      </div>

      <!-- コメント欄 -->
      <div class="row marketing my-5 mx-0 px-3 px-md-0">
        <%= render partial: "assessments/test_review", locals: {test: @test} %>
      </div>
    </main>
</div>